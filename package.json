{
  "name": "boredbot",
  "private": true,
  "version": "1.0.0",
  "author": "Martina Papp",
  "description": "A vanilla JS boredom killer app that fetches random activities using the Bored API and dynamically updates the UI style.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}